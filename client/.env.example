# Recap Studio — client
# Copy this file to .env (Vite only exposes vars prefixed VITE_ to the browser)

# Only needed if the client dev server isn't proxying to the backend itself
# (see vite.config.js's proxy block) — leave unset for normal local dev.
# VITE_API_URL=http://localhost:5002
